# TaskMate – Task Tracker App

A simple terminal-based task manager built with Python. Tasks are stored in a JSON file and persist after exit.

## Features
- Add tasks
- Mark tasks as done
- Delete tasks
- Store tasks in `tasks.json` locally

## Usage

```bash
python taskmate.py